# Pusher Channels credentials — create a free app at https://dashboard.pusher.com
# and fill these in as .env.local (not committed). Multiplayer racing (private
# rooms and public quick-match) will not work until these are set.

PUSHER_APP_ID=
NEXT_PUBLIC_PUSHER_KEY=
PUSHER_SECRET=
NEXT_PUBLIC_PUSHER_CLUSTER=
